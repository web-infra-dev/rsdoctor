export enum UpdateType {
  New = 'New',
  Changed = 'Changed',
  Deleted = 'Deleted',
  NotChanged = 'Not Changed',
}

export enum SortType {
  Name = 'Sorted by Name',
  Size = 'Sorted by Size',
  Delta = 'Sorted by Delta',
}
